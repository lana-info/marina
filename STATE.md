# Состояние проекта

## Текущий этап

MVP локально реализован и проверен. Публикация и production deployment не выполнялись.

## Что уже существует

- веб-приложение Marina с двумя калькуляторами;
- описание продукта и конкурентного контекста во входном материале;
- управляющие документы проекта;
- математическая спецификация MVP в `docs/MATH_CONTRACT.md`.

## Завершено

- определён MVP из двух калькуляторов: плотность и петли/ряды;
- выбраны RU и EN на первом запуске;
- выбран локальный первый запуск без обязательной публикации;
- выбран спокойный, тёплый, читаемый мобильный стиль;
- определено, что результат должен быть проверяемым практическим расчётом;
- проведён независимый review через `sol_architect` без изменения файлов.
- создано точное calculation core на `BigInt`;
- создан мобильный RU/EN UI;
- добавлены localStorage history и PWA service worker/manifest;
- создан и принят `docs/MATH_CONTRACT.md`;
- после исправлений проведён повторный независимый review `sol_architect`: APPROVE.

## Выполняется

- ничего не выполняется; локальная MVP-проверка завершена.

## Принятые решения

- продукт: PWA;
- размер означает размер готового полотна, не мерку тела;
- вычисления выполняет детерминированное ядро, не AI;
- история хранится локально;
- публичный деплой и публикация не входят в текущий scope;
- математический контракт должен быть определён до UI.

## Актуальные метрики

- реализованных калькуляторов: 2 из 2;
- завершённых задач: 5 из 5;
- подтверждена общая модель: да;
- критических блокеров реализации: 0.

## Результаты проверок

- multi-agent инструменты доступны;
- доступны `luna_worker`, `terra_worker`, `sol_architect`;
- репозиторий проверен, исходные материалы в рабочей папке отсутствуют;
- `sol_architect` подтвердил реалистичность архитектурной рамки и выявил обязательные математические решения.
- `terra_worker` создал спецификацию и исправил замечания reviewer;
- финальный `sol_architect` review T-001: APPROVE;
- `git diff --check`: успешно.
- `npm test`: 2 test files passed;
- мобильный Chromium: RU/EN, оба режима, расчёт, история и offline reload проверены;
- browser console: 0 errors, 0 warnings.

## Известные риски

- неоднозначность раппорта, смещения и кромочных;
- правила округления на границе `.5`;
- различие 10 см и 4 дюймов;
- терминология RU/EN;
- обновление service-worker cache;
- ограничения браузерного локального хранилища.

## Следующий полезный шаг

Передать изменения через GitHub при явном разрешении пользователя на push; production deployment не выполнять.
-
## Подготовка VDS и GitHub

- Проект: Marina; GitHub repository: `lana-info/marina`.
- Основная ветка после первичной публикации: `main`.
- VDS/Linux — основная среда разработки; Windows — Windows-specific и визуальная проверка.
- GitHub — точка передачи между средами; одновременно одну ветку изменяет только одна сторона.
- Подготовлены переносимые `AGENTS.md`, `.codex/agents/*.toml`, `scripts/bootstrap_vds.sh` и `VDS_WORKFLOW.md`.
- Публичный репозиторий и push разрешены пользователем отдельно.

## Блокеры VDS-передачи

- До создания первого commit remote и GitHub-ветка отсутствуют.
- В текущем проекте нет исходного продукта, manifest зависимостей или baseline tests; bootstrap это сообщает и пропускает.
- Результат публикации и SHA появятся после commit/push.

## Следующий полезный шаг

Создать и проверить один подготовительный commit, создать публичный `lana-info/marina`, отправить ветку `main`, затем проверить совпадение локального и origin SHA.
-
## Фактическое состояние после публикации

- Публичный GitHub repository создан: `https://github.com/lana-info/marina`.
- Основная ветка: `main`; upstream: `origin/main`.
- Подготовительный commit: `b99ba8893b83c039c5e77102fbb30adddb5c8a6b`.
- Локальный HEAD и `origin/main` совпадают.
- Рабочая копия чистая.
- T-000 (VDS-first инфраструктура) завершена.
- Исходного продуктового кода, dependency manifests и baseline tests в репозитории нет; это не ошибка bootstrap, а текущее состояние нового проекта.

## Следующий полезный шаг

На VDS клонировать `main` в `~/projects/marina`, выполнить `chmod +x scripts/bootstrap_vds.sh && ./scripts/bootstrap_vds.sh`, затем продолжить T-002 — calculation core.

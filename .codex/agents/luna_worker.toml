name = "luna_worker"
description = "Fast worker for narrow, mechanical, documented tasks and known checks."
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
sandbox_mode = "workspace-write"

developer_instructions = """
Execute only the assigned bounded task. Do not expand scope or change architecture.
Do not touch unrelated files. Before finishing, inspect the diff, run assigned checks,
list changed files, and report any unverified result or remaining risk.
"""
